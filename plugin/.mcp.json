{
  "mcpServers": {
    "nexusmind": {
      "command": "npx",
      "args": ["-y", "@smart-coder-labs/nexusmind-mcp@0.7.1"],
      "env": {
        "NEXUSMIND_API_KEY": "${NEXUSMIND_API_KEY}",
        "NEXUSMIND_BASE_URL": "${NEXUSMIND_BASE_URL}"
      }
    }
  }
}
